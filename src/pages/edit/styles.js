import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      flex: 1,
    },

    title: {
      padding: '5%',
      fontWeight: 'bold',
      fontSize: 20,
    },

    body: {
      flex: 1,
      padding: '5%',
    },
    
  });

  export default styles;
  